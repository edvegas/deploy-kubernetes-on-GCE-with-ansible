---

# install client tools (cfssl, cfssljson and kubectl)
- include: 02-client-tools.yml

# provision compute resources
- include: 03-compute-resources.yml

# provision a Certificate Authority and generate TLS certificates
- include: 04-certificate-authority.yml
